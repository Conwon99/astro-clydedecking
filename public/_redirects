# Netlify redirects - Astro builds static HTML files, no SPA fallback needed

# Preserve SEO equity from the old flat location URLs, now under /locations/
/glasgow      /locations/glasgow    301
/edinburgh    /locations/edinburgh  301
/stirling     /locations/stirling   301

# Bot/crawler convenience
/robots.txt   /robots.txt   200
/sitemap.xml  /sitemap.xml  200
/favicon.ico  /favicon.ico  200
